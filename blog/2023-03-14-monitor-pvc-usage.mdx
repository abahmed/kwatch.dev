---
slug: monitor-pvc-usage
title: Monitor Kubernetes PVC usage with kwatch
authors: [abahmed]
tags: [kwatch, kubernetes, monitoring, alerting, pvc]
keywords: [Kubernetes PVC monitoring, persistent volume alerts, disk usage, kwatch]
description: Monitor Kubernetes PersistentVolumeClaims with kwatch and receive alerts before storage pressure becomes an incident.
image: /img/kwatch-logo-full.png
pagination_prev: null
---

A full PersistentVolumeClaim can take an application down even when every pod
looks healthy. kwatch's PVC monitor watches usage and sends a warning or critical
alert before the volume reaches capacity.

<!--truncate-->

## Configure PVC monitoring

The interactive manager is the supported installation and configuration path:

```bash
/bin/bash -c "$(curl -fsSL https://kwatch.dev/kwatch.sh)"
```

Choose **Configure settings**, then enable or tune the PVC monitor. The
equivalent settings are:

```yaml
pvcMonitor:
  enabled: true
  interval: 5
  threshold: 80
  criticalThreshold: 90
  clearThreshold: 75
```

`threshold` starts a warning, `criticalThreshold` raises severity for a more
serious condition, and `clearThreshold` prevents a flapping alert from
resolving too quickly. See the [PVC monitor section](/docs/general-configuration#-pvc-monitor)
for the full setting reference.

## Choose an alert channel

The manager can configure Teams, Slack, PagerDuty, email, webhooks, and other
destinations. Every credential is stored in a Kubernetes Secret and referenced
from the config file, for example:

```yaml
alert:
  teams:
    webhook: "${file:/config/teams-webhook}"
```

Use the [channel guides](/docs/channels) to choose a provider and the
[complete provider reference](/docs/channels/providers) to see every field.

## Verify the alert

After the manager reports the workload as ready, check the PVC monitor metrics
and wait for the next configured sweep. The example screenshots show the
original Teams notification flow:

<p align="center">
  <img src="/img/blog/kwatch-pvc/1.png" alt="Configure PVC monitoring for kwatch" />
</p>

<p align="center">
  <img src="/img/blog/kwatch-pvc/2.png" alt="kwatch PVC usage alert" />
</p>

You can validate the configuration with `kwatch lint`. Keep thresholds below
your storage provider's hard limit and pair PVC alerts with a runbook explaining
how your team expands or cleans up the volume.
