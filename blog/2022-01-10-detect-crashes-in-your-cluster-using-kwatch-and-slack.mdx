---
slug: detect-crashes-in-your-cluster-using-kwatch-and-slack
title: Detect Kubernetes crashes with kwatch and Slack
authors: [andrew]
tags: [kwatch, kubernetes, monitoring, alerting, slack]
keywords: [Kubernetes crash alerts, Slack Kubernetes alerts, pod monitoring, kwatch, webhook]
description: Set up kwatch with Slack to receive clear Kubernetes incident alerts with the cause, evidence, and next step.
image: /img/kwatch-logo-full.png
pagination_prev: null
---

When a Kubernetes workload restarts, a Slack notification is useful only if it
helps you decide what to do next. kwatch turns crash signals, events, and recent
logs into a focused incident message.

This guide shows how to connect Slack and install kwatch with the supported
interactive manager. The manager keeps the webhook in a Kubernetes Secret; do
not paste it directly into `config.yaml`.

<!--truncate-->

## 1. Create a Slack incoming webhook

Create or choose the channel where you want incidents to arrive, then create an
incoming webhook from the [Slack API app settings](https://api.slack.com/messaging/webhooks).
The screenshots below follow the Slack setup flow used by the original guide.

<p align="center">
  <img src="/img/blog/kwatch-slack/1.png" alt="Create a Slack channel" />
</p>

<p align="center">
  <img src="/img/blog/kwatch-slack/2.png" alt="Create a Slack app" />
</p>

<p align="center">
  <img src="/img/blog/kwatch-slack/3.png" alt="Enable an incoming webhook" />
</p>

<p align="center">
  <img src="/img/blog/kwatch-slack/4.png" alt="Choose the Slack channel for the webhook" />
</p>

Copy the webhook URL. Treat it as a credential and keep it out of shell history,
Git, and plain YAML files.

## 2. Install kwatch with `kwatch.sh`

Run the manager from a machine with `kubectl` access to your cluster:

```bash
/bin/bash -c "$(curl -fsSL https://kwatch.dev/kwatch.sh)"
```

Choose **Configure notification**, select Slack, and paste the webhook when the
manager asks for it. It writes a Secret-backed file reference such as:

```yaml
alert:
  slack:
    webhook: "${file:/config/slack-webhook}"
```

The manager then installs the release, waits for the workload to become ready,
and sends a startup notification. Use `kwatch.sh status` whenever you want to
inspect the managed installation.

## 3. Test the notification

Use `kwatch lint --check` for provider validation where supported. For a real
message, enable `healthCheck.diagnostics` in the configuration and call the
`/test-alert` endpoint. See the [Slack channel guide](/docs/channels/slack) for
bot-token mode, routing, retry, and fallback options.

<p align="center">
  <img src="/img/blog/kwatch-slack/5.png" alt="kwatch startup notification in Slack" />
</p>

When a workload fails, kwatch sends the incident reason and available evidence
to the configured channel:

<p align="center">
  <img src="/img/blog/kwatch-slack/6.png" alt="kwatch Kubernetes crash alert in Slack" />
</p>

## Next steps

Add routes if Slack should receive only production or high-severity incidents,
or configure a fallback provider for delivery failures. The [complete provider
reference](/docs/channels/providers) lists every supported alert field.

If you like kwatch, give it a star on [GitHub](https://github.com/abahmed/kwatch)
and share feedback in [Discord](https://discord.gg/kzJszdKmJ7) or the
[issue tracker](https://github.com/abahmed/kwatch/issues).
